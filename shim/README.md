# slicot (transitional package)

This package has been renamed to **ctrlsys**.

```bash
pip install ctrlsys
```

Installing `slicot` will pull in `ctrlsys` automatically, but you should update your imports:

```python
# Old
import slicot

# New
import ctrlsys
```
